pipeline {
  agent {
    docker { image 'nginx: latest' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'nginx --version'
        sh 'docker run --name Test -p 80:80 -d nginx'
        sh 'sleep 300'
        sh 'docker stop Test'
      }
    }
  }
}
