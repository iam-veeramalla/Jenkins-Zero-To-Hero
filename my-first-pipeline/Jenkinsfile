pipeline {
  def branchName = "${env.BRANCH_NAME}"
  if(branchName == 'main'){
  ...
}
  agent {
    docker { image 'node:15-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
