pipeline {
  agent {
    docker { image 'nginx' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'nginx -v'
        sh 'docker run --name Test -p 80:80 -d nginx'
        sh 'sleep 600'
        sh 'docker stop Test'
      }
    }
  }
}
